#include <iostream>
#include <memory>
#include <stack>

void printStack(std::stack<int> stk) {
  while (!stk.empty()) {
  statements
  }
}

int main() {
  auto ps1 = std::make_shared<std::stack<int>>();
  auto ps2 = std::make_shared<std::stack<int>>();

  for (int i = 0; i < 10; ++i) {
    ps1->push(i);
  }

  return 0;
}
