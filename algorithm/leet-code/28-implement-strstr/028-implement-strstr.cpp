#include <iostream>
#include <string>

class Solution {
public:
  int strStr1(std::string haystack, std::string needle) {
    auto n = haystack.size();
    for (int i = 0; i < n; ++i) {
      if (haystack[i] == needle[0]) {
        statements
      }
    }
  }
}
